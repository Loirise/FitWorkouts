<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Fit Workouts</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <div class="d-flex flex-column flex-lg-row align-items-start align-items-lg-center justify-content-lg-between w-100">
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/trainingplans">Training Plans</a>
            </li>
            <% if (currentUser) { %>
            <li class="nav-item">
              <a class="nav-link" href="/trainingplans/new">Create new plan</a>
            </li>
            <% } %>
          </ul>
          <div class="d-flex flex-row align-items-center">
          <% if (!currentUser) { %>
            <a class=" btn btn-info  me-2" href="/login">Login</a>
            <a class=" btn btn-info" href="/register">Register</a>
          <% } else { %>
            <div class="me-3 p-0">
              <p class="text-muted p-0 m-0">Logged in as, <%= currentUser.username %></p>
            </div>
            <form action="/logout" method="POST"><button class="btn btn-info">Logout</button></form>
          <% } %>
            
          </div>
        </div>
      </div>
    </div>
</nav>