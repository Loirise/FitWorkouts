<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Creating Plan</title>
</head>
<body>
    <nav>
        <a href="/trainingplans">Go back to all plans</a>
    </nav>
    <h1>New Training Plan</h1>
    <form action="/trainingplans" method="POST">
        <div>
            <label for="title">Title: </label>
            <input type="text" name="trainingplan[title]" id="title">
        </div>
        <div>
            <label for="price">Price: </label>
            <input type="text" name="trainingplan[price]" id="price">
        </div>
        <div>
            <label for="duration">Duration: </label>
            <input type="text" name="trainingplan[duration]" id="duration">
        </div>
        <div>
            <label for="level">Level: </label>
            <input type="text" name="trainingplan[level]" id="level">
        </div>
        <% for(let i=1; i < 13; i++){ %>
            <div>
                <label for="<%= `${i}[]` %>">Exercise: </label>
                <select name="<%= `${i}[]` %>" id="<%= `${i}[]` %>">
                        <option selected disabled>Choose exercise</option>
                    <% for(let exercise of exercisesList){ %>
                        <option value="<%= exercise %>"><%= exercise %></option>
                    <% } %>
                </select>
                <label for="<%= `${i}[]` %>">Reps: </label>
                <select name="<%= `${i}[]` %>" id="<%= `${i}[]` %>">
                    <option selected disabled>Choose rep count</option>
                    <% for(let j=5; j < 26; j++){ %>
                        <option value="<%= j %>"><%= j %></option>
                    <% } %>
                </select>
            </div>
        <% } %>
        <button type="submit">Create new training plan</button>
    </form>
    
</body>
</html>